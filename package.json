{
    "name": "project_ctrlaltinnovate",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "install": "cd frontend && npm install && cd ../backend && npm install",
      "build": "cd backend && npm run build-frontend",
      "heroku-postbuild": "cd backend && npm start",
      "dev": "cd backend && npm run dev"
    },
    "engines": {
      "node": ">=14.x",
      "npm": ">=7.x"
    }
  }